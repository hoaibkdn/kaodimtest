import React, { memo } from 'react'
import "./../styles/FormHeader.css"

const FormHeader = memo((props) => {
  return (
    <div className="form-header">
      <h2>Title</h2>
    </div>
  )
})

export default FormHeader
